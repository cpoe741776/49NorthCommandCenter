[build]
  command = "npm run build"
  functions = "netlify/functions"
  publish = "build"
  environment = { SECRETS_SCAN_OMIT_PATHS = "*.md,SOCIAL_MEDIA_*.md" }

[functions]
  node_bundler = "esbuild"